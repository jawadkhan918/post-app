import { StyleSheet } from 'react-native';
import { rhp, rwp } from '../../constants';

const styles = StyleSheet.create({
  imgBack: {
    height: rhp(25),
    width: rwp(25),
  },
});
export default styles;
